# icon-lab

Scratch branch for the locbridge app icon. `tools/icon_lab.py` generates
`AppIcon.icon`; every push renders it with Apple's Liquid Glass renderer into
`previews/` (see `.github/workflows/render-icon.yml`). Nothing here is part of
the app until it is copied to `main`.
